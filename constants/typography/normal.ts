/**
 * Global normal type scale token.
 * Used by buttons, navigation labels, pills, and standard copy.
 */
export const normalText = {
  fontSize: 16,
  fontWeight: '600' as const,
  letterSpacing: 0.2,
  lineHeight: 22,
};
